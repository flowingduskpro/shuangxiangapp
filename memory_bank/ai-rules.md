# AI 工程规则（最终版占位，等待你提供的最终文本覆盖）

> 说明：你在 PR-1 里要求将 `memory-bank/ai-rules.md`（你已提供的最终规则文本）加入仓库。
> 
> 当前仓库中存在的权威规则文件为：`memory_bank/ShuangxiangApp AI roles.md`。
> 
> 我已先创建该文件作为占位与接入点，避免 CI/目录结构缺失；请把“最终规则文本”粘贴/替换到本文件中（保持 Markdown 结构即可）。

## 约束
- 不引入业务实现
- 不复制/生成依赖库内部代码
- 维持可审计、可复现、可机器校验的 Gate 与产物

