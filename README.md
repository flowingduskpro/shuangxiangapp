# shuangxiangapp

本仓库当前主要包含：

- `memory_bank/`：产品与工程门禁（CI Gate）文档（必读）
- `ci/`：CI 门禁配置（白名单、MVP scope 规则等）
- `ci_artifacts/`：CI 输出产物目录（由 GitHub Actions 上传 artifact）

如果你在 IDE 里看不到这些目录，请先：

1. 右键项目根目录 → **Synchronize**
2. 打开 Git 工具窗口确认当前分支为 `main`
3. 必要时执行 **File → Invalidate Caches / Restart**

