name: gates (deprecated)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: This workflow is deprecated
        run: |
          echo "This workflow has been replaced by .github/workflows/ci-gates.yml" 
          echo "Please use the CI Gates workflow." 
